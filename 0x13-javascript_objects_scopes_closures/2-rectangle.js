#!/usr/bin/node
// Rectangle class with constructor & input sanitization

module.exports = class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
};
